{% extends 'baseC.html.twig' %}
{% block body %}
	<div class="card border-primary mb-3" style="width:27rem;">
		<div class="card-header">Cuisine</div>
		<div class="card-body">

			<div class="cont">
				<table class="table table-hover">
					<thead>
						<tr>
							<th scope="col">Produit</th>
							<th scope="col">Qté</th>
						</tr>
					</thead>
					{% if prods|length > 0 %}
						{% for element in prods %}
							<tbody>
								<tr class="table-active" id="tab"  >
									<th scope="row">{{ element.produit.nomProd }}</th>
									<td>{{ element.quantite }}</td>


								</tr>
							</tbody>
						{% endfor %}
					{% endif %}
				</table>
			</div>
		</div>

	</div>

<script>




</script>

{% endblock %}
